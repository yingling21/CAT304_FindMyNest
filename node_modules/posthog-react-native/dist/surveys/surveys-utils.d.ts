import { Survey, SurveyQuestion, SurveyQuestionBranchingType, MultipleSurveyQuestion, SurveyAppearance, SurveyQuestionDescriptionContentType } from '@posthog/core';
/**
 * Utility function to determine if a description should be rendered based on content type
 * Only renders text content, skips HTML content for React Native compatibility
 * Defaults to Text content type when not specified
 * @param description The description text
 * @param contentType The content type (text or html), defaults to Text if not provided
 * @returns True if the description should be rendered, false otherwise
 */
export declare function shouldRenderDescription(description?: string | null, contentType?: SurveyQuestionDescriptionContentType): boolean;
export declare const defaultBackgroundColor: "#eeeded";
export type SurveyAppearanceTheme = Omit<Required<SurveyAppearance>, 'widgetSelector' | 'widgetType' | 'widgetColor' | 'widgetLabel' | 'shuffleQuestions'>;
export declare const defaultSurveyAppearance: SurveyAppearanceTheme;
export declare const getDisplayOrderQuestions: (survey: Survey) => SurveyQuestion[];
export declare const hasEvents: (survey: Survey) => boolean;
export declare const canActivateRepeatedly: (survey: Survey) => boolean;
/**
 * Use the Fisher-yates algorithm to shuffle this array
 * https://en.wikipedia.org/wiki/Fisher%E2%80%93Yates_shuffle
 */
export declare const getDisplayOrderChoices: (question: MultipleSurveyQuestion) => string[];
/**
 * Determines the next question to show based on the survey's branching logic
 * @param survey The survey object
 * @param currentQuestionIndex The current question index
 * @param response The user's response to the current question
 * @returns The index of the next question or SurveyQuestionBranchingType.End if the survey should end
 */
export declare function getNextSurveyStep(survey: Survey, currentQuestionIndex: number, response: string | string[] | number | null): number | typeof SurveyQuestionBranchingType.End;
export declare function getContrastingTextColor(color: string): 'black' | 'white';
